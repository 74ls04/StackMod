#!/usr/bin/env python

import os
import RPi.GPIO as GPIO
import time
#from tfmini import TFmini

# port.flushInput()
# port.flushOutput()
#lidar = TFmini('/dev/ttyAMA0')

while True:
    # (d, s, q) = lidar.read()
    # print('Distance: {:5}'.format(d))
    # print d
    # time.sleep(.5)
